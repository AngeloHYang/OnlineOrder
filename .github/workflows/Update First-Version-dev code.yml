on:
    pull_request:
        branches:
          - First-Version-dev
jobs:
    testWorkflow:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v2

            - name: Hello
              run: |
                  echo "Hello, world!"
